# SAIUGOL - Mastodon Bot for Soccer Leagues

# Mastodon Configuration (Required)
MASTODON_INSTANCE=https://mastodon.social
MASTODON_ACCESS_TOKEN=your_access_token_here
# MASTODON_ACCESS_TOKEN_FILE=/run/secrets/mastodon_access_token  # file path containing the token (e.g. for Docker secrets)

# League Configuration (Required)
# Comma-separated list of league codes to monitor.
# Supported: bra.1, eng.1, eng.2, esp.1, ger.1, ita.1, fra.1, usa.1, usa.nwsl, 
#            uefa.champions, uefa.europa, fifa.world, mex.1, ned.1, por.1, sco.1, 
#            conmebol.libertadores
# Default: bra.1
LEAGUE_CODES=bra.1,fifa.world

# Bot Configuration
POLL_INTERVAL_MS=60000     # Fallback polling interval (min: 10000)
DRY_RUN=false              # Set to true for testing without posting
# Elastic polling (adapts to match state):
# POLL_INTERVAL_LIVE_MS=60000           # When any match is live (default 60s, min 10s)
# POLL_INTERVAL_ALERT_MS=120000         # When matches are pre, none live (default 2min, min 30s)
# POLL_INTERVAL_HIBERNATION_MS=1800000  # When all post or no matches (default 30min, min 5min)
# Favorite team(s) for special alerts (e.g. "Galo" highlight on goals/cards):
# FAVORITE_TEAM_NAMES=Atlético Mineiro,Atletico Mineiro
# FAVORITE_TEAM_IDS=123                 # ESPN team ID(s), comma-separated
# REQUEST_TIMEOUT_MS=10000   # API request timeout (min: 1000, max: 60000)

# Optional: Timing Configuration (uncomment to override defaults)
# DELAY_BETWEEN_POSTS_MS=2000              # Delay between posts (min: 500)
# DELAY_BETWEEN_THREAD_POSTS_MS=1000       # Delay between thread posts (min: 500)
# DELAY_BEFORE_HIGHLIGHTS_MS=30000         # Delay before fetching highlights (min: 5000)
# STATE_SAVE_INTERVAL_MS=300000            # State persistence interval (min: 60000)

# Optional: Retry Configuration (uncomment to override defaults)
# RETRY_MAX_ATTEMPTS=3                     # Max retry attempts (1-10)
# RETRY_INITIAL_DELAY_MS=1000              # Initial delay between retries (min: 100)
# RETRY_MAX_DELAY_MS=10000                 # Max delay between retries (min: 1000)

# Optional: Cache Configuration (uncomment to override defaults)
# CACHE_SCOREBOARD_TTL_MS=30000            # Scoreboard cache TTL (min: 5000)
# CACHE_DETAILS_TTL_MS=60000               # Match details cache TTL (min: 5000)
# CACHE_EVENTS_TTL_MS=30000                # Events cache TTL (min: 5000)
# CACHE_HIGHLIGHTS_TTL_MS=120000           # Highlights cache TTL (min: 10000)

# Timezone for scoreboard date (IANA, e.g. America/Sao_Paulo)
# Use league local time to match ESPN API. Default: UTC
TIMEZONE=America/Sao_Paulo

# Language Configuration (see docs/linguagens-validas.md)
# DEFAULT_LANGUAGE: only pt-BR has a dictionary included. Other codes need dictionaries in src/services/dictionaries/.
DEFAULT_LANGUAGE=pt-BR

# ESPN API: Brazilian Portuguese event descriptions (goals, subs, cards text in PT)
# When true, uses site.web.api.espn.com with lang=pt&region=br for summary/events.
# ESPN_USE_PT_DESCRIPTIONS=false

# State Directory (for Docker volume mounting)
# STATE_DIR=/app/data
